# AUTO-POST THREADS MATI TOTAL - JANGAN POSTING APAPUN.
name: Promo Threads (DISABLED)
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'AUTO-POST THREADS MATI TOTAL. Dilarang posting tanpa persetujuan user.'
          exit 1
